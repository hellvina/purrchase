import { createLive, deleteLive } from './live'


describe('Live', () => {
  describe('Create Live', () => {
    test('returns success if pass correctly data', () => {

    });
  })

  describe('Edit live', () => {
    test('change title', () => {

    })
    test('change description', () => {

    })
  })

  describe('Delete live', () => {
    test('delete live if user is the criator and have permissions',() => {
    });
  })

  describe('Show live details', () => {
    test('show live statuses', () => {
    });
  })
})

